```javascript
  ...

  output: {
    filename: "js/[name]-[chunkhash].js",
    clean: true,
  },

  ...

  devtool: process.env.WEBPACK_SERVE ? "eval-cheap-module-source-map" : false,
  cache: { type: process.env.WEBPACK_SERVE ? "memory" : "filesystem" },

```
